#include "max_binary_stack.h"       
       
/**       
 * stack_to_sorted_array - convert a Binary Max Heap a sorted array of integers       
 * @stack: pointer to the root node of the stack to convert       
 * @size: address to store the size of the array       
 *       
 * Return: integer array in sorted descending order       
*/       
int *stack_to_sorted_array(stack_t *stack, size_t *size)       
{       
	(void)stack;       
	(void)size;       
       
	return (0);       
}       
